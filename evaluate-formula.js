function evaluateFormula(graphID) {
    // ChatGPTが生成したコード
    // const formula = document.getElementById(`formulaInput-${graphID}`).value;
    // let sum_f_i = 0;

    // csvData.forEach(row => {
    //     try {
    //         // Evaluate f_i for each row
    //         const f_i = math.evaluate(formula, row);
    //         row.f_i = f_i;
    //         sum_f_i += f_i;
    //     } catch (e) {
    //         console.error('Invalid formula:', e);
    //         document.getElementById('result').textContent = 'Invalid formula';
    //         return;
    //     }
    // });

    // // Calculate w_i and r_class
    // let r_class = 0;
    // csvData.forEach(row => {
    //     row.w_i = row.f_i / sum_f_i;
    //     r_class += row.w_i * row.r_i;
    // });

    // // Display result
    // let resultHtml = `<p>r_class: ${r_class}</p><table border="1"><tr><th>P_i</th><th>N_i</th><th>f_i</th><th>w_i</th><th>r_i</th></tr>`;
    // csvData.forEach(row => {
    //     resultHtml += `<tr><td>${row.P_i}</td><td>${row.N_i}</td><td>${row.f_i}</td><td>${row.w_i}</td><td>${row.r_i}</td></tr>`;
    // });
    // resultHtml += '</table>';
    // document.getElementById('result').innerHTML = resultHtml;
}